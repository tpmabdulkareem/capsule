import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  tabContainer: {
    backgroundColor: '#FFF',
  },
  inActiveTab: {
    color: 'grey',
  },
  activeTab: {
    color: 'black',
  },
});

export default styles;
