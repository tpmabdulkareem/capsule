import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
    height: '100%',
    width: '100%',
    resizeMode: 'cover',
  },
  video: {
    position: 'absolute',
    height: '100%',
    width: '100%',
  },
});
export default styles;
